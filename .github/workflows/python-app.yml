name: Python sample app

on:
  pull_request:
  push:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: python-app-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: python-app
    env:
      NEXT_TELEMETRY_DISABLED: 1
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version-file: python-app/.node-version
          package-manager-cache: false
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.12'
      - name: Install Python runtime dependencies
        run: |
          python -m venv .venv
          .venv/bin/python -m pip install -r runtimes/requirements.txt
      - name: Enable the pinned pnpm version
        run: corepack enable
      - name: Install locked app and lab-test dependencies
        run: |
          pnpm install --frozen-lockfile
          pnpm --dir ../labs install --frozen-lockfile
      - name: Install Chromium and system libraries
        run: pnpm playwright:install:with-deps
      - name: Lint, typecheck, test, and build
        run: pnpm check
      - name: Test Python worker and input cleanup without a desktop
        run: .venv/bin/python -m unittest discover -s runtimes -p "test_*.py"
      - name: Check app integration with the shared labs
        run: pnpm --dir ../labs test:python
      - name: Smoke test production runner and console
        env:
          OPENAI_API_KEY: ''
          CUA_RESPONSES_MODE: fallback
          HOST: 127.0.0.1
          PORT: '4041'
          RUNNER_BASE_URL: http://127.0.0.1:4041
        run: |
          node apps/runner/dist/index.js > /tmp/runner.log 2>&1 &
          runner_pid=$!
          (cd apps/demo-web && exec node node_modules/next/dist/bin/next start --hostname 127.0.0.1 --port 3041) > /tmp/web.log 2>&1 &
          web_pid=$!
          cleanup() {
            result=$?
            trap - EXIT
            kill "$runner_pid" "$web_pid" 2>/dev/null || true
            wait "$runner_pid" "$web_pid" 2>/dev/null || true
            if [ "$result" -ne 0 ]; then cat /tmp/runner.log /tmp/web.log; fi
            exit "$result"
          }
          trap cleanup EXIT
          curl --fail --retry 30 --retry-connrefused --retry-delay 1 http://127.0.0.1:4041/health
          node --input-type=module <<'JS'
          import assert from 'node:assert/strict';
          const base = process.env.RUNNER_BASE_URL;
          const scenarios = await fetch(`${base}/api/scenarios`).then(r => r.json());
          assert.equal(scenarios.length, 3);
          assert(scenarios.every(scenario => scenario.defaultPrompt && scenario.workspaceTemplatePath));
          assert.equal(await fetch(`${base}/api/runs/active`).then(r => r.json()), null);
          JS
          curl --fail --retry 30 --retry-connrefused --retry-delay 1 http://127.0.0.1:3041/ > /tmp/web.html
